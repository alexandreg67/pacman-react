import type { GameState } from '../types'

// Phase 1 will implement real movement; for now, no-op integration points

export function stepGhosts(state: GameState): GameState {
  // Placeholder: return state unchanged for Phase 0
  return state
}
